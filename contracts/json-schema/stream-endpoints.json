{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "destinations": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "endpoint": {
            "additionalProperties": false,
            "properties": {
              "host": {
                "description": "The SRT listener's host. Absent for a WHIP or WHEP endpoint, whose `url`\n carries it.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "node": {
                "type": "string"
              },
              "port": {
                "format": "uint16",
                "maximum": 65535,
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "url": {
                "description": "What a peer dials: `srt://host:port`, or the WHIP or WHEP URL.",
                "type": "string"
              }
            },
            "required": [
              "node",
              "url"
            ],
            "type": [
              "object",
              "null"
            ]
          },
          "id": {
            "maxLength": 63,
            "minLength": 1,
            "pattern": "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$",
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "ingress": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "description": "The SRT listener's host. Absent for a WHIP or WHEP endpoint, whose `url`\n carries it.",
          "type": [
            "string",
            "null"
          ]
        },
        "node": {
          "type": "string"
        },
        "port": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "url": {
          "description": "What a peer dials: `srt://host:port`, or the WHIP or WHEP URL.",
          "type": "string"
        }
      },
      "required": [
        "node",
        "url"
      ],
      "type": [
        "object",
        "null"
      ]
    }
  },
  "required": [
    "destinations"
  ],
  "title": "StreamEndpoints",
  "type": "object"
}

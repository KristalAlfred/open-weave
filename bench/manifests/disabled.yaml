# Disabled stream: accepted and listed but never provisioned (status idle).
name: disabled
enabled: false
source:
  srt:
    node: strom-node-1
    latency: 200
destinations:
  - srt:
      node: strom-node-2
      latency: 1000

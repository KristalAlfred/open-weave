# Source pinned to a node that never registers: stays pending until it does
# (Kubernetes-style unschedulable), never provisions.
name: unplaceable
enabled: true
source:
  srt:
    node: strom-node-404
    latency: 200
destinations:
  - srt:
      node: strom-node-2
      latency: 1000
